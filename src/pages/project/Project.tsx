import ProjectForm from "@/components/project/ProjectForm";

const Project = () => {
  console.log(`프로젝트 페이지 실행`);

  return (
    <div>
      <ProjectForm/>
    </div>
  );
};

export default Project;
